fruits = ("banana","pear", "orange")

#Prints out if it finds apple. 
for x in fruits:
  if x == "apple":
    print("Omg, i'd really like to eat that " + x)
    break 
#prints if not found apple. (but does so 3 times)
  else:
     print("Fuck, no apple")
